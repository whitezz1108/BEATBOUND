Album art for the song-select screen.

Drop a cover here named `<level id>.jpg` (e.g. `toosie_slide_arena_primary.jpg`),
roughly square, 256x256 or larger. The title screen picks it up automatically;
until then the song name is shown in the slot.
